import JunkMetadataFieldResolver from "./metadata";
import SwapJunkToArcadePartsResolver from "./swap_junk_to_arcade_parts";

const CustomJunkResolvers = [
  JunkMetadataFieldResolver,
  SwapJunkToArcadePartsResolver,
];

export default CustomJunkResolvers;
